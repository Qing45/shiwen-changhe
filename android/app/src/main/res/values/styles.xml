<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 基础主题：状态栏 / 工具栏主色统一为 app 暗夜墨色（#050818），
         避免出现「AppCompat 默认紫色 + WebView 暗蓝色」的视觉断层。 -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <!-- 启动主题：Theme.SplashScreen 是 Android 12+ 推荐的冷启动背景承载；
         windowSplashScreenBackground 给新版用，android:windowBackground 给旧版兜底。
         切到主 Activity（AppTheme.NoActionBar）后由 WebView 接管并显示渐变背景。 -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>